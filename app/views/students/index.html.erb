<div>
  <% @students.each do |student| %>
  <%binding.pry%>
    <div><%= student.first_name + " " + student.last_name %></div>
    <!-- <a href= '<%=student_path(student)%>'>see this student</a> -->
    <%= link_to student.first_name student_path(student) %>
  <% end %>
</div>
